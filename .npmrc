# npm configuration
# This project uses npm, not pnpm
# omit=optional would skip optional dependencies (like platform-specific binaries)
# but might break packages that need them, so we leave it commented
# omit=optional
