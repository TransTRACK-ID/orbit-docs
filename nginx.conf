server {
    listen 3031;
    server_name _;

    client_max_body_size 50M;

    # ============================================
    # Orbit Docs (port 3000) served under /docs
    # ============================================
    location /docs/ {
        proxy_pass         http://orbit-docs-app:3000/docs/;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    # ============================================
    # OAuth discovery metadata for MCP clients
    # (must precede the / catch-all and hidden-file deny rules)
    # ============================================
    location ^~ /.well-known/ {
        proxy_pass         http://orbit-docs-app:3000/docs/.well-known/;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # ============================================
    # OAuth authorization + token endpoints for MCP
    # ============================================
    location ^~ /oauth/ {
        proxy_pass         http://orbit-docs-app:3000/docs/oauth/;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # ============================================
    # Orbit Docs (port 3000) served under /docs
    # ============================================
    # ============================================
    # Orbit App (port 3000) — everything else
    # ============================================
    location / {
        proxy_pass         http://orbit-app:3000/;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
