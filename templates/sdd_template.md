# System Design Document (SDD): {{PROJECT_NAME}}

**Generated By:** {{AGENT_NAME/VERSION}}
**Date Generated:** {{CURRENT_DATE}}
**Repository Target:** {{REPO_URL_OR_PATH}}

---

## 1. Architecture Overview
> **Agent Instruction:** Analyze the root structure, `docker-compose.yml`, and configuration files (e.g., `nuxt.config.ts`, `next.config.js`). Provide a high-level architectural summary of how the client, server, and infrastructure components interact.

{{Architecture Summary}}

---

## 2. Component & Service Design
> **Agent Instruction:** Scan the backend/server directories (e.g., `server/api/`, `src/controllers/`) and frontend components. Break down the system into its primary technical services.

### 2.1 Client Application
*   **Framework:** {{Framework Name & Version}}
*   **Routing Strategy:** {{e.g., File-based routing, SPA, SSR}}
*   **Styling System:** {{e.g., TailwindCSS, Styled Components}}

### 2.2 Server / API Layer
*   **Runtime:** {{e.g., Node.js, Bun, Nitro}}
*   **Architecture Pattern:** {{e.g., RESTful, Microservices, Event-Driven}}
*   **Key Services:**
    *   `{{Service 1}}`: {{Technical responsibility}}
    *   `{{Service 2}}`: {{Technical responsibility}}

### 2.3 Agent / ML Subsystems (If Applicable)
> **Agent Instruction:** Search for LLM SDKs, custom agent loops, Python scripts, or containerized runners. Detail the prompt strategies, tool bindings (e.g., MCP), and isolation mechanisms.
*   **Engine:** {{e.g., LangChain, Custom Loop, Python Subprocess}}
*   **Integration Points:** {{How the main server communicates with the agent, e.g., SSE, WebSockets}}

---

## 3. Data Storage & Management
> **Agent Instruction:** Scan ORM configuration (e.g., Drizzle, Prisma, Mongoose) and caching layers.

### 3.1 Primary Database
*   **Engine:** {{e.g., PostgreSQL, SQLite}}
*   **ORM/Query Builder:** {{Tool used}}
*   **Key Schemas:**
    *   `{{Table 1}}`: {{Core columns and indexes}}
    *   `{{Table 2}}`: {{Core columns and indexes}}

### 3.2 Caching & State Persistence
*   **Mechanism:** {{e.g., Redis, In-Memory, LocalStorage}}
*   **Usage:** {{What data is cached and TTL strategies}}

---

## 4. API Contracts & Data Flow
> **Agent Instruction:** Scan for defined types, interfaces, or tRPC routers. Map out the data flow for the most critical data exchange in the application.

### 4.1 Data Flow Example: {{Critical Process, e.g., Creating a Workspace}}
1.  **Client:** {{Payload structure sent from client}}
2.  **API Route:** `{{Route Path}}` (Method: `{{HTTP Method}}`)
3.  **Controller/Logic:** {{Brief description of data validation and database mutation}}
4.  **Response:** {{Expected success/failure payload}}

---

## 5. Deployment & Infrastructure
> **Agent Instruction:** Analyze CI/CD pipelines (e.g., `.gitlab-ci.yml`, `.github/workflows/`), Dockerfiles, and deployment scripts.

*   **Build Process:** {{Commands used to build the artifacts}}
*   **Containerization:** {{Summary of Docker multi-stage builds or environment isolation}}
*   **CI/CD Pipeline:** {{Stages of the pipeline, e.g., Lint -> Test -> Build -> Deploy}}
